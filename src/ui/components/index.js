const head = require('./head');
const header = require('./header');
const apiKeyField = require('./apiKeyField');
const languageSelector = require('./languageSelector');
const installButton = require('./installButton');
const freeSources = require('./freeSources');
const footer = require('./footer');

module.exports = { head, header, apiKeyField, languageSelector, installButton, freeSources, footer };